---
trigger: always_on
---

FILE AND CODE ORGANIZATION
- All child component will be created into app/components, folder name will same as route folder name, ex: interviewers route will have interviewers folder in components
- components/shared will include all common components for both public and admin 
- If screen has button, avatar then use component from app/components/ui
- Layout use from common layout: header, footer. Don't generate new component of layout